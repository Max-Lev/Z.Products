define(['app', 'itemCntrl', 'ScrollDir','imagesService'], function (app) {    'use strict';    app.controller('productCntrl',        ['$scope', '$state', '$stateParams','imagesService',        function ($scope, $state, $stateParams,imagesService) {            var item, Product;            console.log('productCntrl init');            $scope.name = 'ProductCntrl';            imagesService.getUser();            item = $state.data.product;            Product = function (item) {                this.id = item.id;                this.name = item.name;                this.url = item.url;                this.brand = item.brand;                this.productsList = item.productsList;            };            $scope.Product = new Product(item);            $scope.navItem = function (item) {                $state.data = {item: item};                $state.go('product.item', {itemID: item.id});            };        }]);});