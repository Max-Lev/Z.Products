define(['app'], function (app) {    app.directive('productData', ['$compile', function ($compile) {        return {            restrict: 'AE',            scope: {                ///two-way data-binding                productItemData: '=product',                // prodPrice:'@prodPrice'            },            link: function (scope, elem, attr) {                console.log(scope.productItemData);                scope.prodPrice = '1000$';                debugger;                scope.$watch('prodPrice',function(){                },true);                $compile(element.contents())(scope);                // scope.$apply();                //scope.$emit();            }        }    }]);});