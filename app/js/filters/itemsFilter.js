define(['app'], function (app) {    app.filter("items", [function () {        return function (productNames, list) {            var filterList = [];            list.forEach(function (v, i) {                if (v.name.toLowerCase().indexOf(productNames.toLowerCase())!=-1) {                    filterList.push(v);                }            });            return filterList;        }    }]);});